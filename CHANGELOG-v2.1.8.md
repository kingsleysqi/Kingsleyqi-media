## 📢 版本概览
**版本号**: 2.1.8  

- 2.1.8 **媒体管理与转码稳定性优化**：新增后台媒体管理删除功能，增强媒体编辑与删除操作；修复音乐播放时残留播放列表显示、临时网盘下载链接释放导致的失败；转码状态检测使用当前配置的转码服务器；R2 转码输出路径新增目录确认提示。

## 🔧 变更内容
- `admin.html`：新增媒体管理删除入口与编辑弹窗“删除”按钮。
- `admin.html`：修复媒体管理卡片操作入口，避免播放列表残留在切换其他媒体时显示。
- `admin.html`：转码队列和状态检测逻辑改为使用当前保存的转码服务器地址。
- `admin.html`：R2 转码输出目录确认对话框，自动补全斜杠并提醒选择目录而非文件路径。
- `admin.html`：修复临时网盘下载逻辑，延迟释放 Blob URL，避免浏览器过早撤销导致下载失败。
- `package.json`：版本升级至 `2.1.8`。
- `version.js`：前端版本号同步为 `2.1.8`。
- `README.MD`：版本徽章和版本更新日志同步为 `2.1.8`。

## ✅ 兼容说明
- 向后兼容现有配置和文件结构。
- 本次更新主要为后台管理与转码流程优化，不会影响前台播放器核心行为。
