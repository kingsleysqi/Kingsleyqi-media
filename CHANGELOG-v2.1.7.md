## 📢 版本概览
**版本号**: 2.1.7  

- 2.1.7 **临时网盘直链下载修复**：修复后台临时网盘“下载”按钮的直接下载逻辑，确保 `Authorization` 鉴权正常传递；避免浏览器在同域下载时因预签名/重定向问题触发异常；增强下载失败提示。
- 同步更新 `package.json`、`version.js` 与 `README.MD` 中的版本号。

## 🔧 变更内容
- `admin.html`：优化临时网盘下载按钮逻辑，改为先发起带认证头的 `/api/admin/download` 请求，再将返回数据导出为 Blob 下载文件。
- `package.json`：版本升级至 `2.1.7`。
- `version.js`：前端版本号同步为 `2.1.7`。
- `README.MD`：版本徽章和版本更新日志同步为 `2.1.7`。

## ✅ 兼容说明
- 向后兼容现有配置和文件结构。
- 该修复仅影响临时网盘下载行为，不会更改其他媒体播放或上传逻辑。
