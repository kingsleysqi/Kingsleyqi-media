my-media-library/                  ← 项目根目录（GitHub 仓库根目录）
├── index.html                     ← 主页面（美观动态前端，已更新域名）
├── functions/
│   └── api/
│       └── list.js                ← API 函数：动态读取 R2 文件列表（已更新域名）
├── public/                        ← 可选：如果有额外静态资源（如 favicon、CSS 等）
│   └── favicon.ico
├── wrangler.jsonc                 ← 可选但推荐：Wrangler 配置文件（绑定 R2）
├── package.json                   ← 可选（如果以后加构建工具）
└── .gitignore